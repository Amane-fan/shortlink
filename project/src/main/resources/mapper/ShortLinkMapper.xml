<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.usts.shortlink.project.dao.mapper.ShortLinkMapper">

    <select id="groupShortLinkCount" resultType="com.usts.shortlink.project.dto.resp.ShortLinkCountQueryDTO">
        SELECT
            gid,
            COUNT(*) AS shortLinkCount
        FROM t_link
        WHERE enable_status = 1
          AND del_flag = 0
          AND gid IN
        <foreach collection="list" item="gid" open="(" separator="," close=")">
            #{gid}
        </foreach>
        GROUP BY gid
    </select>
</mapper>
